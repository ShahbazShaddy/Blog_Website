import { makeStyles } from "@mui/styles";
export const useStyles = makeStyles({
  font: {
    fontFamily: "Roboto !important",
    color: "#000000", // Update text color for light theme
  },
  darkFont: {
    fontFamily: "Roboto !important",
    color: "#E0E0E0", // Update text color for dark theme
  },
});